node_modules/
dist/
.env
*.log
prd.json

# Local tool settings, and the OneDrive conflict copies it accumulates.
.claude/

# User data lives in the platform data directory now (XSP_DATA_DIR overrides it),
# not next to the code. Kept ignored so an older checkout does not commit one.
server/data/

# CI writes this so the run can be checked for dropped test files.
vitest-results.json

# Coverage reports are build output; the threshold lives in vitest.config.ts.
coverage/
